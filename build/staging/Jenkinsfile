pipeline {
    agent any

    environment {
        PPE_REPO = 'staging-ppe'
    }

    stages {
        stage('Promote tag components'){
            steps {
                script {

                    moveComponents destination: "${PPE_REPO}", nexusInstanceId: 'nxrm3', tagName: "${TAG}"

                }
            }
        }
    }
}

